# Python binary used by the webapp prediction bridge
# (defaults to .venv/bin/python if not set)
PYTHON_BIN=

# Absolute path to the project data root (defaults to repository root)
SOLAR_DATA_ROOT=

# Override model for the webapp (leave blank to use manifest default)
MODEL_NAME=
